import { SelectionRangePipe } from './selection-range.pipe';

describe('SelectionRangePipe', () => {
  it('create an instance', () => {
    const pipe = new SelectionRangePipe();
    expect(pipe).toBeTruthy();
  });
});
